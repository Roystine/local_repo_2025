name: workflow2

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:  # allows manual run

jobs:
  print:
    runs-on: ubuntu-latest

    steps:
      - name: Print HelloWorld
        run: echo "helloworld from second workflow"
